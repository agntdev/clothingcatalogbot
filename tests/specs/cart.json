[
  {
    "name": "/cart opens an empty cart",
    "steps": [
      { "send": { "text": "/cart" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Корзина пуста. Добавьте товар из каталога." } }] }
    ]
  },
  {
    "name": "cannot add a removed product to cart",
    "steps": [
      { "send": { "callback": "cart:add:missing-product" }, "expect": [{ "method": "answerCallbackQuery" }, { "method": "editMessageText", "payload": { "text": "Этот товар больше недоступен. Выберите другой товар в каталоге." } }] }
    ]
  }
]
