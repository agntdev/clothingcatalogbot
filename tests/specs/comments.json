[
  {
    "name": "comment entry rejects a removed product",
    "steps": [
      { "send": { "callback": "comment:start:missing-product" }, "expect": [{ "method": "answerCallbackQuery" }, { "method": "editMessageText", "payload": { "text": "Этот товар больше недоступен. Выберите другой товар в каталоге." } }] }
    ]
  }
]
