[
  {
    "name": "/admin is protected when owner access is not configured",
    "steps": [
      {
        "send": { "text": "/admin" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Owner access isn't set up yet." } }]
      }
    ]
  },
  {
    "name": "admin button is protected when owner access is not configured",
    "steps": [
      {
        "send": { "callback": "admin:open" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Owner access isn't set up yet." } }]
      }
    ]
  }
]
